Site assets that aren't generated. `logo.png` (white on transparent) goes here;
`build_site.py` copies it into the header, the navigation, and the signature
sheets. Without it the header shows the organisation name in text.
